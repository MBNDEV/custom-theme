<?xml version="1.0"?>
<ruleset name="Custom Theme">
    <description>WordPress Coding Standards for Custom Theme</description>

    <!-- Scan theme files only (exclude vendor) -->
    <file>.</file>

    <!-- WordPress PHP standards -->
    <rule ref="WordPress-Core"/>
    <rule ref="WordPress-Extra"/>
    <rule ref="WordPress-Docs"/>

    <!-- Indent: 2 spaces (override WordPress default of tabs) -->
    <arg name="tab-width" value="2"/>
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent">
        <severity>0</severity>
    </rule>
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="2"/>
            <property name="tabIndent" value="false"/>
        </properties>
    </rule>
    <rule ref="PEAR.Functions.FunctionCallSignature">
        <properties>
            <property name="indent" value="2"/>
        </properties>
    </rule>
    <exclude name="WordPress.WhiteSpace.PrecisionAlignment"/>
    <rule ref="Universal.WhiteSpace.PrecisionAlignment">
        <severity>0</severity>
    </rule>

    <!-- Ignore vendor, build, and scripts (e.g. security scan) -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>build/*</exclude-pattern>
    <exclude-pattern>scripts/*</exclude-pattern>

    <!-- CLI -->
    <arg value="p"/>
    <arg name="colors"/>
</ruleset>
